
const { LogAfter } = require('./log-after.annotation');
const { LogBefore } = require('./log-before.annotation');
const { LogError } = require('./log-error.annotation');

module.exports = {
    LogAfter, LogBefore, LogError
}